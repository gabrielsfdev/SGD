class LoginModel:
    def __init__(self):
        # Lógica de autenticação e validação de dados vai aqui
        pass

    def authenticate(self, username, password):
        if username == 'magno' and password == '123':
            return True
